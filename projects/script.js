fetch('./projects.json').then(response=>{if(!response.ok)throw new Error('Unable to load projects');return response.json()}).then(projects=>{document.getElementById('project-grid').innerHTML=projects.map(project=>`<article class="card"><img src="../assets/images/projects/${project.image}" alt="${project.alt}"><div class="body"><div class="meta"><span>${project.category}</span><span>${project.date}</span></div><h2>${project.name}</h2><p>${project.description}</p></div></article>`).join('')}).catch(()=>{document.getElementById('project-grid').textContent='Project details are temporarily unavailable.'});
